# deterministic-neural-video-codec
A deterministic INT16 refactor of DCVC-RT. Solves cross-platform bit-drift between ARM and x86 architectures with custom CUDA kernel fusion and ~178ms P-frame latency.
